<% content_for :title, "Create Event — Signal Fire Admin" %>

<div class="max-w-4xl">
  <p class="text-xs font-mono text-stone/50 uppercase tracking-widest mb-1">Admin · Internal</p>
  <h1 class="font-serif text-3xl text-ink mb-6">Create event.</h1>
  <%= render "form", event: @event %>
</div>
