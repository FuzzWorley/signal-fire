<% content_for :title, "Edit Event — Signal Fire" %>

<div class="mb-1">
  <%= link_to "← Events", host_events_path, class: "text-sm text-stone hover:text-ink transition" %>
</div>

<div class="mb-6">
  <p class="font-mono text-[10px] uppercase tracking-widest text-stone mb-1">Edit Event</p>
  <h1 class="font-serif text-3xl text-ink"><%= @event.title %></h1>
</div>

<%= render "form", event: @event %>
