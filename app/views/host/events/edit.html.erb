<% content_for :title, "Edit Event — Signal Fire" %>

<div class="max-w-2xl">
  <div class="mb-6">
    <h1 class="font-serif text-3xl text-ink">Edit event</h1>
    <p class="text-stone text-sm mt-1 truncate"><%= @event.title %></p>
  </div>

  <%= render "form", event: @event %>
</div>
