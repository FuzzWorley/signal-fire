<% content_for :title, "New Event — Signal Fire" %>

<div class="max-w-2xl">
  <div class="mb-6">
    <h1 class="font-serif text-3xl text-ink">New event</h1>
    <p class="text-stone text-sm mt-1">It will be immediately visible on the totem board.</p>
  </div>

  <%= render "form", event: @event %>
</div>
