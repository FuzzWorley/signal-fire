<% content_for :title, "New Event — Signal Fire" %>

<div class="mb-1">
  <%= link_to "← Events", host_events_path, class: "text-sm text-stone hover:text-ink transition" %>
</div>

<div class="mb-6">
  <p class="font-mono text-[10px] uppercase tracking-widest text-stone mb-1">New Event</p>
  <h1 class="font-serif text-3xl text-ink">Create a new event.</h1>
  <p class="text-stone text-sm mt-1">It'll appear on the board as soon as you save.</p>
</div>

<%= render "form", event: @event %>
